{
    "$id": "urn:gregor:data-model:1.12:experiment_rna_short_read",
    "$schema": "http://json-schema.org/draft-07/schema#",
    "definitions": {},
    "properties": {
        "RIN": {
            "description": "RIN number for quality of sample",
            "examples": [
                "9.8"
            ],
            "type": "number"
        },
        "analyte_id": {
            "type": "string",
            "x-references": "> analyte.analyte_id",
            "x-required-condition": "CONDITIONAL (rna_sample_type != isogenic_cell_line)"
        },
        "date_data_generation": {
            "description": "Date of data generation (First sequencing date)\n\nNotes: Can be missing if RC receives external data; ISO 8601 date format",
            "examples": [
                "2022-06-29"
            ],
            "type": "string"
        },
        "estimated_library_size": {
            "description": "Calculated size factors for the sample, which are relative scaling factors to account for library size differences. Such factors may be calculated with functions such as estimateSizeFactors() in the R bioconductor package.",
            "examples": [
                "364"
            ],
            "type": "number"
        },
        "experiment_rna_short_read_id": {
            "description": "identifier for experiment_rna_short_read (primary key)\n\nNotes: RCs make their own IDs, must begin with center abbreviation as defined in participant table; need to be globally unique in consortium; may be generated by prepending experiment_sample_id with center abbreviation",
            "examples": [
                "Broad_E1",
                "Broad_E2",
                "GSS201938-01-021-SG-1"
            ],
            "type": "string",
            "x-primary-key": true
        },
        "experiment_sample_id": {
            "description": "identifier used in the data file (e.g. the SM tag in a BAM header, column headers for genotype fields in a VCF file)",
            "examples": [
                "12339D-SA"
            ],
            "type": "string"
        },
        "experiment_type": {
            "items": {
                "description": "single-end or paired-end? targeted or untargeted RNA-seq experiment?",
                "enum": [
                    "single-end",
                    "paired-end",
                    "targeted",
                    "untargeted"
                ],
                "examples": [
                    "paired-end|targeted"
                ],
                "type": "string"
            },
            "type": "array"
        },
        "library_prep_type": {
            "items": {
                "description": "type of library prep",
                "enum": [
                    "stranded poly-A pulldown",
                    "stranded total RNA",
                    "rRNA depletion",
                    "globin depletion",
                    "custom"
                ],
                "examples": [
                    "stranded total RNA|globin depletion"
                ],
                "type": "string"
            },
            "type": "array"
        },
        "prep_targets_detail": {
            "items": {
                "description": "depletion kit or list of depletion targets used\n\nNotes: include this field if library_prep_type = 'custom'",
                "type": "string"
            },
            "type": "array",
            "x-required-condition": "CONDITIONAL (library_prep_type = custom)"
        },
        "read_length": {
            "description": "sequenced read length (bp); GREGoR RCs do paired end sequencing, so is the example of 100bp indicates 2x100bp.\n\nNotes: contact the DCC if not doing paired-end or you wish to share data with unknown read length.",
            "examples": [
                "100"
            ],
            "type": "integer"
        },
        "rna_sample_type": {
            "description": "indicates whether experiment_rna_short_read_id corresponds to study sample with analyte_id or an isogenic cell line",
            "enum": [
                "GREGoR_sample",
                "isogenic_cell_line"
            ],
            "examples": [
                "GREGoR_sample"
            ],
            "type": "string"
        },
        "seq_library_prep_kit_method": {
            "description": "Library prep kit used\n\nNotes: Can be missing if RC receives external data",
            "examples": [
                "Kappa Hyper PCR plus,",
                "Kappa Hyper PCR free"
            ],
            "type": "string"
        },
        "sequencing_platform": {
            "description": "sequencing platform used for the experiment\n\nNotes: Can be missing if RC receives external data",
            "examples": [
                "HiSeq2000",
                "HiSeq2500",
                "HiSeqX",
                "NovaSeq"
            ],
            "type": "string"
        },
        "single_or_paired_ends": {
            "description": "single or paired end",
            "enum": [
                "single-end",
                "paired-end"
            ],
            "type": "string"
        },
        "total_reads": {
            "description": "total number of reads\n\nNotes: float type required for large number, but value entered should be an integer",
            "examples": [
                "58,112,692"
            ],
            "type": "number"
        },
        "within_site_batch_name": {
            "description": "batch number for the site, important for future batch correction",
            "examples": [
                "Batch 4"
            ],
            "type": "string"
        }
    },
    "required": [
        "experiment_rna_short_read_id",
        "experiment_type",
        "read_length",
        "single_or_paired_ends",
        "sequencing_platform",
        "within_site_batch_name"
    ],
    "title": "experiment_rna_short_read",
    "type": "object",
    "version": "1.12",
    "x-table-required": "CONDITIONAL (aligned_rna_short_read, aligned_rna_short_read_set, readcounts_rna_short_read)"
}
