{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "https://raw.githubusercontent.com/UCI-GREGoR/GREGor_dashboard/blob/main/server/utilities/v1.7/json_schemas/experiment_optical_mapping.json",
    "title": "experiment_optical_mapping",
    "version": "1.7",
    "type": "object",
    "required": [
        "experiment_optical_mapping_id",
        "analyte_id",
        "experiment_sample_id",
        "isolation_protocol",
        "labeling_protocol"
    ],
    "definitions": {},
    "properties": {
        "experiment_optical_mapping_id": {
            "type": "string",
            "description": "identifier for experiment_optical_mapping (primary key)\n\nNotes: RCs make their own IDs, must begin with center abbreviation as defined in participant table; need to be globally unique in consortium; may be generated by prepending experiment_sample_id with center abbreviation",
            "examples": [
                "Broad_E1",
                "Broad_E2",
                "GSS201938-01-021-SG-1"
            ]
        },
        "analyte_id": {
            "type": "string"
        },
        "experiment_sample_id": {
            "type": "string",
            "description": "identifier used in the data file (e.g. the SM tag in a BAM header, column headers for genotype fields in a VCF file)\n\nNotes: may be the same as experiment_optical_mapping_id if the file does contain sample identifiers\nshould be present if downstream file contains a sample_id (e.g. BAM, VCF)\nsome centers have one id for the sample (tube) and a diff ID for the sample as named in the VCF; experiment_sample_id = ID in the VCF file; analyte_id = ID for the tube/aliquot/whatever",
            "examples": [
                "CLIA-000026"
            ]
        },
        "isolation_protocol": {
            "type": "string",
            "description": "Name of DNA Isolation kit used",
            "examples": [
                "Bionano Prep SP Blood & Cell Culture DNA Isolation Kit (80042)"
            ]
        },
        "labeling_protocol": {
            "type": "string",
            "description": "Name of DNA labeling kit used",
            "examples": [
                "Direct Label and Stain-G2 (DLS-G2) Kit (80046)"
            ]
        },
        "date_data_generation": {
            "type": "string",
            "description": "Date of data generation (First sequencing date)\n\nNotes: Can be missing if RC receives external data; ISO 8601 date format",
            "examples": [
                "2022-06-29"
            ]
        },
        "DNA_concentration": {
            "type": "number",
            "description": "DNA concentration in ng/uL\n\nNotes: before labeling",
            "examples": [
                "112"
            ]
        },
        "mapping_platform": {
            "type": "string",
            "description": "OGM platform used for the experiment",
            "examples": [
                "Saphyr"
            ]
        }
    }
}